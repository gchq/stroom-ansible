# Define the upstream host(s) that are running stroom-proxy-rmeote
{% for host in groups['stroom_remote_proxy_stack'] %}
server {{ hostvars[host]['inventory_hostname'] }}:<<<STROOM_PROXY_PORT>>>;
{% endfor %}
